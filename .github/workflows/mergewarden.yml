name: MergeWarden

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - edited
      - labeled
      - unlabeled
      - ready_for_review

concurrency:
  group: mergewarden-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  mergewarden:
    name: MergeWarden
    runs-on: ubuntu-latest
    steps:
      - name: Run MergeWarden
        uses: sjh9714/mergewarden/packages/action@main
        with:
          config: mergewarden.yml
          mode: warn
          fail-on-block: "false"
          comment: "false"
