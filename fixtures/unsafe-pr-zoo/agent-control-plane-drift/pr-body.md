Agent control-plane drift replay fixture.

Updates the agent instructions alongside the feature work. The instruction file steers every future agent run in this repository, which is why it is called out separately here.
