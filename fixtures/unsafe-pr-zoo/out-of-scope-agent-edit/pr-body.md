<!-- mergewarden-contract
version: 1
agent: codex
task: update auth session handling
allowed_paths:
  - src/auth/**
-->

Implements the retry budget the contract declares, and also touches a billing file outside the declared scope.
