Add a read-only, fixed-prompt documentation review workflow.

Adds a reviewed prompt step to the agent workflow. The prompt text is checked in rather than read from pull-request input, so nothing a contributor writes reaches the model.
