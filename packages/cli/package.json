{
  "name": "agent-gate",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-gate": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/main.ts --format esm --dts --sourcemap",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agent-gate/core": "workspace:*"
  }
}
