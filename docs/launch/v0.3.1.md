# Agent Gate v0.3.1 Launch Kit

This is the copy-ready launch package for the first seven days after the
v0.3.1 release. Every external post is published manually from the maintainer's
own account. Do not request stars, votes, comments, or endorsements.

## Fact Sheet

- Category: checkout-free change-control layer for AI-generated pull requests.
- Runtime: deterministic TypeScript; no runtime LLM calls.
- Trust boundary: policy is loaded from the exact base commit.
- Execution boundary: no checkout or execution of pull-request code.
- First use: scan a public pull request through `npx` without installing the
  Action.
- Evidence: rule ID, severity, path, canonical evidence, finding ID, policy
  digest, base/head SHA, and collection completeness.
- Limitation: not a semantic code reviewer or a complete workflow taint engine.
- Public proof: [composite PR #16](https://github.com/sjh9714/agent-gate-install-smoke-20260617/pull/16)
  and its [SHA-pinned Action run](https://github.com/sjh9714/agent-gate-install-smoke-20260617/actions/runs/29071622785).

## Core Story

An AI agent can correctly edit the requested documentation and still change a
release workflow, agent instructions, MCP configuration, or package lifecycle
script in the same pull request. Tests and semantic review answer whether the
code works. Agent Gate records whether the change crossed an agreed repository
boundary.

## Seven-Day Publishing Order

| Day | Action                                                               | Completion evidence                      |
| --- | -------------------------------------------------------------------- | ---------------------------------------- |
| 0   | Merge launch-readiness docs and update GitHub issues                 | Green `main` CI and current issue links  |
| 1   | Submit Show HN and remain available for three hours                  | Submission URL and feedback notes        |
| 2   | Send three individual design-partner messages                        | Three sent-message URLs or private notes |
| 3   | Publish the DEV article                                              | Article URL                              |
| 5   | Check `r/devsecops` rules and post only if self-promotion is allowed | Post URL or recorded skip reason         |
| 6   | Publish the Korean Disquiet post                                     | Post URL                                 |
| 7   | Record metrics and choose follow-up work from observed evidence      | Completed Day 7 table                    |

Do not publish all channels on the same day. Fold earlier questions and
misunderstandings into later posts.

## Show HN

**Title:** Show HN: Agent Gate – checkout-free policy checks for AI-generated PRs

**URL:** https://github.com/sjh9714/Agent-Gate

Hacker News link submissions cannot carry a separate body. Publish the text
below as the first comment immediately after submitting the repository URL.

### First comment

An earlier version of Agent Gate was posted here on June 14. At that point it
was mostly a deterministic CI firewall. Since then I added a public no-install
`npx` scan, exact-base policy loading, fail-closed collection, differential
workflow checks, and a live checkout-free proof. This post is about that
overhaul rather than a routine version announcement.

I built Agent Gate because I kept coming back to a review question that ordinary
CI does not answer. An AI agent completes the requested change, but the same PR
also touches a release workflow, expands token permissions, changes `AGENTS.md`,
or edits files outside the declared task. Every individual edit may look
reasonable. The uncomfortable part is the combined boundary change.

Agent Gate is a deterministic GitHub Action and CLI for that boundary. It reads
pull-request metadata through the GitHub API, loads policy from the exact base
commit, compares the relevant base/head content, and emits stable evidence. It
does not checkout the PR, execute repository code, or call an LLM at runtime.

You can try it on any public PR without installing anything:

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan owner/repo#123
```

A live composite example is PR #16 in the public smoke repository:
https://github.com/sjh9714/agent-gate-install-smoke-20260617/pull/16

That PR combines an allowed docs edit with an out-of-scope workflow change,
workflow/job permission escalation, and an `AGENTS.md` change. The Action run
uses the exact release commit and performs no checkout:
https://github.com/sjh9714/agent-gate-install-smoke-20260617/actions/runs/29071622785

The trust model is intentionally narrow. Incomplete GitHub file or content
collection fails closed. The agentic-workflow-injection rule handles direct
prompt input and one-hop `env` flow, not general cross-step taint analysis. The
tool also does not claim that a PR is semantically correct or secure.

I would especially value maintainer feedback on two questions: which findings
are useful enough to block after tuning, and which evidence still feels too
noisy to act on?

## DEV Article

**Title:** AI agents can open PRs. Who checks whether they crossed the line?

### The gap between "works" and "stayed in scope"

An AI coding agent can produce a pull request that builds, passes tests, and
implements the requested feature. That same PR can also edit a release
workflow, increase a GitHub token permission, change `AGENTS.md`, or touch a
file outside the declared task.

That does not make the PR malicious. Still, the reviewer should be able to see
the boundary change without reconstructing it from a large diff. Ordinary tests
answer whether the repository still works. A semantic reviewer asks whether the
code makes sense. Neither reliably records whether the PR stayed inside the
agreed scope.

I built [Agent Gate](https://github.com/sjh9714/Agent-Gate) for that narrower
question.

### A checkout-free policy gate

Agent Gate is an open-source GitHub Action and CLI. It collects pull-request
metadata and selected file content through GitHub APIs, loads `agent-gate.yml`
from the exact base commit, and produces deterministic findings.

It intentionally does not:

- checkout or execute pull-request code;
- run target-repository package scripts;
- load policy from the PR head branch; or
- call an LLM at runtime.

That gives the policy a useful trust boundary. A pull request can change the
policy for future PRs, but it cannot weaken the policy evaluating itself.

If GitHub reports 42 changed files and Agent Gate can collect only 41, the
analysis is incomplete and fails closed. Authentication failures, rate limits,
and server errors are not silently treated as missing files or default policy.

### Try it before installing it

Node 20 or newer is enough to scan a public PR:

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan owner/repo#123
```

The CLI clones nothing and executes nothing from the target repository. Human,
JSON, and Markdown output are available, and the Action uses the same analysis
contract.

### What the evidence looks like

The public [composite proof PR](https://github.com/sjh9714/agent-gate-install-smoke-20260617/pull/16)
starts with a docs-only contract and then changes documentation, a workflow,
and `AGENTS.md`. Its [Action run](https://github.com/sjh9714/agent-gate-install-smoke-20260617/actions/runs/29071622785)
reports:

- two `contract/out-of-scope` findings;
- a high-risk workflow path;
- `agent-control-plane/drift` for `AGENTS.md`; and
- workflow- and job-level permission escalation.

The report also records the policy digest, exact base/head SHAs, expected and
analyzed file counts, and whether collection completed. Finding IDs are derived
from canonical evidence rather than display wording, so a waiver can target one
exact finding with a reason and expiry.

### Differential workflow checks

Existing workflow risk should not be re-reported whenever an unrelated line
changes. For modified workflows, Agent Gate compares canonical base/head sets
and reports newly introduced or expanded permissions, unpinned references,
secret references, head checkout patterns, and other configured checks.

Added workflows are compared with an empty base. Deleted workflows are reported
explicitly. Malformed or unavailable content makes the analysis incomplete
instead of producing a partial pass.

### Adopt it without turning CI red on day one

The intended rollout is:

1. **Observe:** collect findings and identify repository-specific noise.
2. **Warn:** require review while the policy and waivers are tuned.
3. **Block:** enforce only the findings whose evidence the team trusts.

Checks can be configured independently as `off`, `warn`, or `error`. Exact,
expiring waivers live in base-branch policy and remain visible in reports.

### Honest limits

Agent Gate is not a replacement for tests, a semantic code reviewer, or a tool
such as zizmor. Its agentic-workflow rule recognizes registered agent actions
and a deliberately narrow set of direct or one-hop prompt flows. Shell, files,
step outputs, and cross-job data flow are outside the v0.3 scope.

It also has no SaaS, GitHub App, product telemetry, or runtime model judgment.
The goal is reproducible change-control evidence, not an opaque risk score.

I am looking for policy and false-positive feedback from repositories where AI
agents create or review PRs. Which findings would you trust enough to block,
and which should remain warnings or be turned off?

## Reddit (`r/devsecops`)

Check the current community rules immediately before posting. If project posts
or self-promotion are not allowed, record the skip and do not substitute a
different subreddit.

**Title:** I built a checkout-free policy gate for AI-generated PRs. Looking for false-positive feedback

AI coding agents can produce a PR that passes tests while also changing a
workflow permission, `AGENTS.md`, MCP configuration, or a file outside the
declared task. That does not automatically make the PR dangerous. I just want
the boundary change to be obvious during review.

I built Agent Gate as a deterministic GitHub Action and CLI for that boundary.
It uses GitHub APIs, loads policy from the exact base commit, and compares the
relevant base/head content. It does not checkout or execute PR code and does not
call an LLM at runtime.

Public PRs can be scanned without installing the Action:

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan owner/repo#123
```

The live proof intentionally combines a docs-only contract with an out-of-scope
workflow edit, permission escalation, and `AGENTS.md` drift:
https://github.com/sjh9714/agent-gate-install-smoke-20260617/pull/16

Incomplete file/content collection fails closed. The workflow analysis is
differential, and individual checks can be observe/warn/block with exact,
expiring waivers.

This is not a semantic reviewer or a complete taint engine. I am interested in
the operational question: would the evidence be useful in a real DevSecOps
review, or would these findings be too noisy? Which one would you tune first?

## Disquiet (Korean)

**제목:** AI가 만든 PR, 테스트 통과 뒤의 '범위 이탈'을 확인하는 오픈소스를 만들었습니다

AI 코딩 에이전트가 요청한 기능을 정확히 구현하고 테스트까지 통과해도,
같은 PR에서 GitHub Actions 권한·`AGENTS.md`·MCP 설정·요청 범위 밖 파일이
함께 바뀔 수 있습니다. 이런 변경이 항상 취약점인 것은 아니지만, 머지 전에
사람이 명확히 확인해야 할 경계 변화라고 생각했습니다.

[Agent Gate](https://github.com/sjh9714/Agent-Gate)는 이 문제만 좁게 다루는
오픈소스 GitHub Action과 CLI입니다. PR 코드를 checkout하거나 실행하지 않고,
GitHub API와 정확한 base commit의 정책을 사용해 base/head 차이를 분석합니다.
런타임 LLM 호출도 없습니다.

설치 없이 공개 PR을 바로 확인할 수 있습니다.

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan owner/repo#123
```

[공개 예제 PR](https://github.com/sjh9714/agent-gate-install-smoke-20260617/pull/16)은
docs-only 범위를 선언한 뒤 workflow 권한과 `AGENTS.md`까지 함께 바꿉니다.
Agent Gate는 범위 이탈, workflow/job 권한 상승, agent control-plane drift를
각각 재현 가능한 finding으로 남깁니다.

아직 semantic code review나 완전한 taint 분석을 대체하려는 도구는 아닙니다.
실제 저장소에서 어떤 finding이 유용하고 어떤 부분이 오탐인지 피드백을 받고
싶습니다. 팀에서 AI PR을 운영한다면 어떤 증거가 있어야 warn에서 block으로
전환할 수 있을까요?

## Week-One Design-Partner Messages

Send these individually from the maintainer's account. Re-open every linked PR
and workflow immediately before sending. Do not post them as unsolicited public
issues and do not imply that a finding is a vulnerability.

### Matomo

Matomo's current [Codex review workflow](https://github.com/matomo-org/matomo/blob/5.x-dev/.github/workflows/codex-review.yml)
already separates preflight, model execution, review posting, and cleanup
permissions.

I ran Agent Gate v0.3.1 against two public Matomo PRs without installing
anything. The workflow-introduction [PR #24660](https://github.com/matomo-org/matomo/pull/24660)
returned `needs-review` with 17 findings, primarily the newly introduced
job-level read/write permission boundaries. A recent ordinary
[PR #24793](https://github.com/matomo-org/matomo/pull/24793) produced a complete
pass with zero findings.

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan matomo-org/matomo#24660
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan matomo-org/matomo#24793
```

I do not consider these vulnerability claims. Does this separation look useful
during workflow review, or are some of the 17 findings noise you would
immediately tune out?

### Composio

Composio's [Changelog to Docs workflow](https://github.com/ComposioHQ/composio/blob/next/.github/workflows/docs.changelog-to-docs.yml)
uses Codex to turn changelog changes into documentation pull requests.

Agent Gate v0.3.1 reports the workflow-conversion
[PR #2723](https://github.com/ComposioHQ/composio/pull/2723) as `needs-review`
with five findings: the `AGENTS.md` control-plane change plus newly introduced
workflow patterns. The generated docs [PR #3458](https://github.com/ComposioHQ/composio/pull/3458)
produces a complete pass with zero findings.

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan ComposioHQ/composio#2723
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan ComposioHQ/composio#3458
```

I am only after a noise check here; no installation or endorsement. Would those
five boundary findings have helped during the workflow review, or would you
configure any of them differently?

### agentclientprotocol/codex-acp

The current [Codex update workflow](https://github.com/agentclientprotocol/codex-acp/blob/main/.github/workflows/codex-update.yml)
uses Codex to finish an update and open a pull request through a GitHub App.

The original workflow [PR #44](https://github.com/agentclientprotocol/codex-acp/pull/44)
produces `needs-review` with five deterministic workflow findings. A recent
bot-generated update [PR #276](https://github.com/agentclientprotocol/codex-acp/pull/276)
produces a complete pass with zero findings.

```bash
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan agentclientprotocol/codex-acp#44
npx --yes @jinhyuk9714/agent-gate@0.3.1 scan agentclientprotocol/codex-acp#276
```

This is not a claim that the workflow is vulnerable. Does reporting the
boundary when the automation is introduced, while staying quiet on a routine
update PR, match the review model you would want?

## Metrics

The Day 0 values were recorded on 2026-07-10. GitHub traffic is a rolling
14-day aggregate and includes automation; it is context, not an adopter count.
The npm downloads endpoint did not yet recognize the newly published scoped
package, so `unavailable` must not be converted to zero.

| Metric                                               |       Day 0 | Day 7 |          30-day target |
| ---------------------------------------------------- | ----------: | ----: | ---------------------: |
| GitHub stars                                         |          11 |     — |                     50 |
| Forks                                                |           1 |     — |                      — |
| Open issues                                          |           5 |     — |                      — |
| External public installs found by GitHub code search |           0 |     — |                      3 |
| Policy-feedback conversations                        |           0 |     — |                      5 |
| Verified false-positive examples                     |           0 |     — |                      — |
| GitHub unique views, rolling 14 days                 |          22 |     — | 300 qualified visitors |
| GitHub unique clones, rolling 14 days                |         341 |     — |           context only |
| npm weekly downloads                                 | unavailable |     — |           context only |

Day 7 success criteria are four channel submissions or documented rule-based
skips, three individual design-partner messages, three policy-feedback
conversations, one public external installation, and 25 stars. Stars and
installs are outcomes to measure, not reasons to claim success when the
underlying evidence is absent.

Record only GitHub traffic, npm downloads, public install search, issues, and
contributors. Do not add product telemetry.
